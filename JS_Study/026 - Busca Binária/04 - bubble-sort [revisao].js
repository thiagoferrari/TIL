function bubbleSort(vetor) {
    let passadas = 0
    let comparacoes = 0
    let totalTrocas = 0
    let trocas

    do {
        passadas++
        trocas = 0 //reboot em trocas

        for (let i = 0; ) {

        }

    } while (trocas > 0)
}